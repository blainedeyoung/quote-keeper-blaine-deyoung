{
  "name": "quote-keeper",
  "version": "1.0.0",
  "description": "A full stack MERN app for keeping and sharing your favorite quotes.  Featuring a token-based authentication process complete \r with protected routes so an unauthorized user can't get past the login screen.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "express-jwt": "^6.0.0",
    "mongoose": "^5.13.5",
    "morgan": "^1.10.0"
  }, "engines": {
    "node": "14.17.3"
  }
}
